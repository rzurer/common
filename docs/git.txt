git init
git add .
git commit -m 'initial commit'

git push origin master

1. rename git repo on github
2. git remote rm origin
3. git remote add origin   https://github.com/rzurer/thumbnailCarousel.git

This is an edit to test git push origin master